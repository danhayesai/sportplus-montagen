---
import NotFoundPage from "../components/pages/NotFoundPage.astro";

// Astro serves this for any unmatched route. German is the default language;
// visitors deep in the /en/ tree still get a usable page with working nav.
---

<NotFoundPage lang="de" />
